<!-- list full url by short form link -->
<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <!-- Add your EJS code here -->
    <% for (shortForm in urls){ %>
      <!-- match the respond id with urlDatabase -->
      <% if ( shortURL === shortForm) { %>
        <li><%= shortForm %>: <%= urls[shortForm] %></li>
        <% } %>
    <% } %>

</body>
</html>