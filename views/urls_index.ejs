<!-- list all short URL and it's full URL -->
<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <!-- Add your EJS code here -->
  <header>
    <% include ./partials/_header %>
  </header>

  <main>
    <!-- // list all URLs -->
    <!--<% for (let shortForm in urls){ %> -->
      <!-- <li><%= shortForm %>:<%= urls[shortForm] %></li> -->
    <!-- <% } %> -->

  <table>
    <!-- // list all URLs -->
    <% for (let shortForm in urls){ %>
      <tr>
          <td><%= shortForm %></td>
          <td>-></td>
          <td><a href="<%= urls[shortForm] %>"><%= urls[shortForm] %></a></td>
          <td>
            <!-- Add delete buttons to each URL object in the index page -->
            <form action="/urls/<%= shortForm %>/delete" method="POST">
              <input type="submit" value="DELETE">
            </form>
          </td>
      </tr>
    <% } %>
  </table>





    <a href="">short</a>
  </main>


</body>
</html>